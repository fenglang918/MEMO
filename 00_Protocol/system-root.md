# System Root（最高指令）

目标：把“转瞬即逝的记忆/经验”转成可长期演化的对象，并保持可迁移、可版本化、可检索。

硬约束：

- 不要写入可直接滥用的明文机密（密码、API Key、私钥、恢复码、完整地址/证件号/银行卡等）
- 优先维护结构与可检索性：对象化 > 流水账堆叠
- 记忆不是存储，是选择：只有能被对象化的内容才进入稳定区

原则：

> 我们选择记录什么，说明我们珍惜什么。

