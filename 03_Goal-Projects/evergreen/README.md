# evergreen

这里放不适合按 `YYYY/MM/` 归档、但需要长期持续维护的目标 / 项目。

适合放在这里的通常是：

- 长期健康项目
- 长期能力建设
- 持续运营中的个人系统
- 没有清晰起始月份、但会长期存在的主题

如果一个项目有明确启动月份、阶段性强，优先仍放回 `YYYY/MM/`。

常见判断：

- 回答的是“这是一个长期维护对象” -> `./`
- 回答的是“这是某个时间窗口里启动的具体项目” -> `YYYY/MM/`
